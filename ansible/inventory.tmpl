[managers]
swarm-manager ansible_hostname=${manager-dns} ansible_host=${manager-ip} # ${manager-id}

[workers]
swarm-workers-1 ansible_hostname=${worker1-dns} ansible_host=${worker1-ip} # ${worker1-id}
swarm-workers-2 ansible_hostname=${worker2-dns} ansible_host=${worker2-ip} # ${worker2-id}

[managers_priv]
${manager-priv-ip}

[workers_priv]
${worker1-priv-ip}
${worker2-priv-ip}
